# Vector Search

Synthetic evaluation fixture for retrieval testing.

Vector search uses pgvector with an HNSW index. The distance metric is cosine distance using vector_cosine_ops. HNSW is configured for approximate nearest-neighbor retrieval.
