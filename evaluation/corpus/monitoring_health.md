# Monitoring and Health

Synthetic evaluation fixture for retrieval testing.

The service exposes a /health endpoint for liveness checks. It exposes /health/db for PostgreSQL connectivity. Langfuse records model latency, token usage, and failures.
