# Rerank Pipeline

Synthetic evaluation fixture for retrieval testing.

The rerank stage uses BAAI/bge-reranker-v2-m3. The rerank timeout is 30 seconds. If rerank fails, the system falls back to the RRF ranking.
