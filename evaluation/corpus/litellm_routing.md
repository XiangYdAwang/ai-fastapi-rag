# LiteLLM Routing

Synthetic evaluation fixture for retrieval testing.

LiteLLM provides a unified model interface. The project uses an OpenAI-compatible SiliconFlow endpoint. Chat and embedding calls use separate model names but can share the same provider key.
