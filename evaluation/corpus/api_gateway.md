# API Gateway

Synthetic evaluation fixture for retrieval testing.

The public API uses the /api prefix. The development rate limit is 60 requests per minute. The default downstream request timeout is 30 seconds.
